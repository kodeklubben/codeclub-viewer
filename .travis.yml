language: node_js
before_install:
  - git clone --depth 1 --branch master https://github.com/kodeklubben/oppgaver.git ../oppgaver
cache: yarn
script:
  - yarn eslint
  - yarn stylelint
  - yarn test
  - ./travis_build.sh

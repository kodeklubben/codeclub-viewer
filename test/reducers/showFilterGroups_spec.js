/*import {expect} from 'chai';
import deepFreeze from 'deep-freeze';

import reducer from '../../src/reducers/filterGroupsCollapse';

describe('filterGroupsCollapse reducer', () => {
  describe('SHOW_FILTERGROUPS', () => {
    it('do nothing', () => {
      const initialState = {};
      const action = {
        type: 'SHOW_FILTERGROUPS',
        payload: {'language': false}
      };

      deepFreeze(initialState);
      deepFreeze(action);
      const nextState = reducer(initialState, action);

      expect(nextState).to.equal(action.payload);
    });
  });
});*/
